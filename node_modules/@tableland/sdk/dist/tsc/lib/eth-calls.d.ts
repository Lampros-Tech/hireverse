import { ContractReceipt } from "ethers";
interface TableRegistration {
    receipt: ContractReceipt;
    tableId: string;
}
declare function registerTable(): Promise<TableRegistration>;
declare function doIOwn(tableId: string): Promise<boolean>;
export { registerTable, doIOwn };
