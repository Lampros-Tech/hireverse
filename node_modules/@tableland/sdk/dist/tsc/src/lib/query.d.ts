/**
 * Send a SQL query to tableland network
 * @param query A SQL query to run
 * @returns If read query, result-set. If write query, nothing.
 */
import { ReadQueryResult, Connection } from "../interfaces";
export declare function query(this: Connection, query: string): Promise<ReadQueryResult | null>;
